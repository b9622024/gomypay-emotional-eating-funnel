import {describe,expect,it} from "vitest";import {getEatingOutPlan} from "./eatingOutMenu";describe("eating out menu",()=>{it("returns complete coach-style meal fields",()=>{const x=getEatingOutPlan("便當店",2);expect(x.meal).toContain("雞腿");expect(x.script).toContain("飯半碗");expect(x.drink).toContain("無糖");expect(x.closing).toBeTruthy();expect(x.rescue).toBeTruthy();expect(x.check).toHaveLength(4)})});
