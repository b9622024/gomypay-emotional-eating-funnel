import { NextResponse } from "next/server";import { z } from "zod";import { prisma } from "@/lib/db";import { authorizeProductAccess } from "@/lib/workbook";const body=z.object({favoriteCategory:z.string().max(50),title:z.string().max(150),content:z.string().max(1000),orderScript:z.string().max(300)});export async function POST(req:Request,{params}:{params:Promise<{accessToken:string}>}){const {accessToken}=await params;if(!await authorizeProductAccess(accessToken,"sugary_drink_swap_pro"))return NextResponse.json({error:"無權存取"},{status:404});const p=body.safeParse(await req.json().catch(()=>null));if(!p.success)return NextResponse.json({error:"收藏資料不正確"},{status:400});const old=await prisma.drinkSwapProFavorite.findFirst({where:{accessToken,title:p.data.title}});const favorite=old??await prisma.drinkSwapProFavorite.create({data:{...p.data,accessToken}});return NextResponse.json({favorite,message:old?"已在收藏中":"✓ 已加入收藏"})}
