import { notFound } from "next/navigation";import { authorizeWorkbook } from "@/lib/workbook";import BreakthroughPlanClient from "@/components/breakthrough/BreakthroughPlanClient";export default async function Page({params}:{params:Promise<{accessToken:string}>}){const {accessToken}=await params;if(!await authorizeWorkbook(accessToken))notFound();return <BreakthroughPlanClient token={accessToken}/>}
