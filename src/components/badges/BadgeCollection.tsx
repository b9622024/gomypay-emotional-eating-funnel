"use client";
import {useState} from "react";
import {badgeConfig,type BadgeConfig} from "@/content/badgeConfig";
import InteractiveBadgeCard from "./InteractiveBadgeCard";
import Badge3DModal from "./Badge3DModal";
export default function BadgeCollection({earnedBadges,entries=[],characterCreated=false,compact=false}:{earnedBadges:string[];entries?:Array<{earnedBadge:string;completedAt?:string|Date|null;actionPointsEarned?:number}>;characterCreated?:boolean;compact?:boolean}){const [selected,setSelected]=useState<BadgeConfig|null>(null);return <section className={`badge-collection ${compact?"compact":""}`}><header><span>ACHIEVEMENT COLLECTION</span><h2>我的成就徽章</h2><p>每一枚徽章都代表你看見了一個新的嘴饞線索。點擊已解鎖徽章，可查看 3D 收藏。</p></header><div className="badge-grid">{badgeConfig.map(badge=>{const entry=entries.find(x=>x.earnedBadge===badge.name),unlocked=badge.levelNumber===0?characterCreated:earnedBadges.includes(badge.name);return <div className={unlocked?"badge-open-3d":""} key={badge.id} onClick={()=>unlocked&&setSelected(badge)}><InteractiveBadgeCard badgeName={badge.name} badgeImageUrl={badge.imageUrl} badgeDescription={badge.description} lockedDescription={badge.lockedDescription} unlocked={unlocked} levelNumber={badge.levelNumber} earnedAt={badge.levelNumber===0?null:entry?.completedAt}/>{unlocked&&<span className="badge-3d-hint">點擊查看真正 3D 模型</span>}</div>})}</div><Badge3DModal badge={selected} onClose={()=>setSelected(null)}/></section>}
