"use client";
export type InteractiveBadgeCardProps={badgeName:string;badgeImageUrl:string;badgeDescription:string;lockedDescription?:string;unlocked:boolean;levelNumber:number;earnedAt?:string|Date|null;actionPoints?:number;celebrate?:boolean};
export default function InteractiveBadgeCard(props:InteractiveBadgeCardProps){
  return <article className={`badge-card ${props.unlocked?"unlocked":"locked"} ${props.celebrate?"celebrate":""}`}><div className="badge-tilt" aria-label={`${props.badgeName}，${props.unlocked?"已解鎖":"尚未解鎖"}`}><img src={props.badgeImageUrl} alt={props.badgeName} loading="lazy" draggable={false}/>{!props.unlocked&&<span className="badge-lock">🔒</span>}</div><div className="badge-copy"><small>{props.levelNumber===0?"第 0 天":`第 ${props.levelNumber} 關`}</small><h3>{props.badgeName}</h3><p>{props.unlocked?props.badgeDescription:props.lockedDescription}</p>{props.unlocked&&props.earnedAt&&<time>解鎖於 {new Date(props.earnedAt).toLocaleDateString("zh-TW")}</time>}</div></article>
}
