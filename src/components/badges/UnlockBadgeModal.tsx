"use client";
import type {BadgeConfig} from "@/content/badgeConfig";
export default function UnlockBadgeModal({badge,onContinue}:{badge:BadgeConfig|null;actionPoints:number;onContinue:()=>void}){if(!badge)return null;return <div className="badge-modal-backdrop" role="dialog" aria-modal="true" aria-label="徽章已解鎖"><section className="badge-modal"><span>ACHIEVEMENT UNLOCKED</span><h2>徽章已解鎖</h2><p>你已獲得「{badge.name}」</p><img className="badge-unlock-image" src={badge.imageUrl} alt={badge.name}/><strong>這枚徽章代表你已經看見一個新的嘴饞線索，離破解迷霧核心又更近一步。</strong><button onClick={onContinue}>關閉視窗</button></section></div>}
