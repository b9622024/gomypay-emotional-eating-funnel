"use client";
import {useState} from "react";
export default function ReplayPlanButton({token}:{token:string}){const [busy,setBusy]=useState(false);async function replay(){if(!confirm("確定要重新開始嗎？角色測驗、第 1～7 關進度與止損地圖都會清除，接著會回到第 0 天重新創建角色。"))return;setBusy(true);const response=await fetch(`/api/breakthrough/${token}/reset`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({fromLevel:0})});if(response.ok)location.href=`/access/${token}/character-creation`;else{alert("目前無法重新開始，請稍後再試");setBusy(false)}}return <button className="bt-reset bt-replay" disabled={busy} onClick={replay}>{busy?"正在重置角色與關卡…":"重新玩一次 7 天破關計畫"}</button>}
