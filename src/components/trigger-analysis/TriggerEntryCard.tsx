import { triggerAnalysisContent as c,type TriggerEntry } from "@/content/cravingTriggerAnalysis";
export function TriggerEntryCard({entry,onEdit,onDelete}:{entry:TriggerEntry;onEdit:()=>void;onDelete:()=>void}){return <article className="ta-entry-card"><div className="ta-entry-top"><time>{entry.occurredAt?new Intl.DateTimeFormat("zh-TW",{month:"numeric",day:"numeric",hour:"2-digit",minute:"2-digit"}).format(new Date(entry.occurredAt)):"未填時間"}</time><span>{entry.didEatOrDrink===false?`✓ ${c.list.yes}`:entry.didEatOrDrink===true?c.list.no:"未記錄結果"}</span></div><h3>{entry.cravingItem||"未填寫想吃／喝的內容"}</h3><dl><div><dt>{c.list.trigger}</dt><dd>{entry.mainTrigger||entry.triggerTypes?.[0]||"尚未分類"}</dd></div><div><dt>{c.list.score}</dt><dd>{entry.cravingScore??"—"} / 10</dd></div></dl><div className="ta-entry-actions"><button type="button" onClick={onEdit}>{c.edit}</button><button className="delete" type="button" onClick={onDelete}>{c.delete}</button></div></article>}
