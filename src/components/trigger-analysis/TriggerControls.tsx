"use client";
import { triggerAnalysisContent as c } from "@/content/cravingTriggerAnalysis";
export function ScoreSlider({label,value,onChange}:{label:string;value:number|null|undefined;onChange:(value:number)=>void}){const current=value??0;return <label className="ta-slider"><span>{label}<strong>{current}</strong></span><input type="range" min="0" max="10" value={current} onChange={e=>onChange(Number(e.target.value))}/><small><i>0</i><i>10</i></small></label>}
export function TriggerTypeSelector({value,onChange}:{value:string[];onChange:(value:string[])=>void}){return <div className="ta-trigger-groups">{c.classify.groups.map(group=><section key={group.title}><h3>{group.title}</h3><div>{group.options.map(option=><label className={value.includes(option)?"selected":""} key={option}><input type="checkbox" checked={value.includes(option)} onChange={e=>onChange(e.target.checked?[...value,option]:value.filter(x=>x!==option))}/><span>{option}</span></label>)}</div></section>)}</div>}
export function IfThenPlanCard({value,onChange}:{value:string;onChange:(value:string)=>void}){return <div className="ta-plans"><p>{c.strategy.label}</p>{c.strategy.examples.map(example=><button className={value===example?"selected":""} type="button" key={example} onClick={()=>onChange(example)}><span>{example}</span><small>{c.strategy.apply}</small></button>)}</div>}
