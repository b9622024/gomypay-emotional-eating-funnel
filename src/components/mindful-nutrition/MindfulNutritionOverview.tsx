import { mindfulNutritionContent as content,type NutritionDayState } from "@/content/mindfulNutritionTracker";
import NutritionFormulaCard from "./NutritionFormulaCard";
export default function MindfulNutritionOverview({days,onSelect}:{days:Record<number,NutritionDayState>;onSelect:(day:number)=>void}){return <><section className="nutrition-hero"><span>7-DAY MINDFUL TRACKER</span><h1>{content.title}</h1><strong>{content.subtitle}</strong><p>{content.intro}</p></section><NutritionFormulaCard/><section className="nutrition-day-grid">{Array.from({length:7},(_,i)=>i+1).map(day=><button className={days[day]?.isCompleted?"completed":""} key={day} onClick={()=>onSelect(day)}><span>DAY {day}</span><strong>{days[day]?.isCompleted?"✓ 已完成":"開始記錄"}</strong><small>觀察今天的營養與嘴饞 →</small></button>)}</section></>}
